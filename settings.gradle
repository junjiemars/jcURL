//rootProject.name = 'NHttpClient'

include 'base'

include 'async'
rootProject.children.find { it.name == 'async' }.name = 'NHttpClient'

include 'sync'

include 'web'
rootProject.children.find { it.name == 'web' }.name = 'NHttpWeb'

